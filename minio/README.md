# MinIO CORS

`cors.json` is a reference for MinIO AIStor (per-bucket `mc cors set`).

MinIO Community uses global CORS via `MINIO_API_CORS_ALLOW_ORIGIN` on the `minio` service in `docker-compose.yml`.
